[package]
name = "analyzer-ffi"
description = "Stable C ABI surface consumed by the platform user interfaces."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true

[lib]
# staticlib for the Xcode link, rlib so Rust tests can exercise the surface.
crate-type = ["staticlib", "rlib"]

[lints]
workspace = true

[dependencies]
analyzer-audio = { workspace = true }
analyzer-dsp = { workspace = true }
analyzer-engine = { workspace = true }
analyzer-plot = { workspace = true }

[build-dependencies]
cbindgen = { workspace = true }
