// Lets Swift `import AnalyzerFFI` without an Xcode bridging header. The header
// itself is generated by cbindgen during `cargo build -p analyzer-ffi`.
module AnalyzerFFI {
    header "analyzer.h"
    export *
}
